# DESIGN.md: 여기찜 (YeogiJjim)

## 1. 무드 (Mood)

**웜 & 코지 (Warm & Cozy)**

저장해둔 맛집을 다시 꺼내보는 아늑한 느낌을 담는다. "찜"이라는 이름처럼 캐주얼하고 친근하면서도, 개인 기록물(다이어리)에 가까운 따뜻한 정서를 지향한다.

- 전체 레이아웃은 **미니멀**하게 — 여백을 넉넉히 두고 불필요한 장식을 최소화
- 형태는 **동글동글한(라운드)** 인상 — 카드, 버튼, 태그 모두 둥근 모서리를 사용하되, 완전한 알약형(pill)까지는 가지 않는 적당한 곡률 유지
- 색은 절제해서 사용하고, 형태(둥근 모서리)로 부드러움과 친근함을 표현

---

## 2. 컬러 (Color)

크림 → 핑크 → 코랄레드로 이어지는 웜톤 그러데이션 팔레트를 사용한다.

| 역할 | 색상명 | HEX | 용도 |
|---|---|---|---|
| 베이스 | 크림 | `#FEEAC9` | 배경 |
| 서브 | 핑크 | `#FDACAC` | 카드, 보조 요소 |
| 포인트 | 코랄레드 | `#FD7979` | 버튼, 태그, 강조 요소 |
| 텍스트 | 웜그레이 | `#4A3F3B` (예시) | 본문 텍스트, 구분선 등 |

**컬러 사용 원칙**
- 완전한 무채색(블랙/그레이) 대신, 브라운이 살짝 섞인 웜그레이를 사용해 전체 톤의 통일감을 유지한다
- 3가지 메인 컬러 + 웜그레이 텍스트, 총 4가지 색상 범위 내에서 절제해서 사용한다

---

## 3. 타이포그래피 (Typography)

| 용도 | 폰트 | 비고 |
|---|---|---|
| 포인트 (제목, 상세 태그 등) | 학교안심 둥근미소 | 둥글게 굴린 자소 형태로 부드럽고 친근한 인상, 밝고 발랄한 분위기를 연출하는 타이틀용 서체. KERIS(한국교육학술정보원) 제작, OFL 라이선스로 자유롭게 이용 가능 |
| 본문 | Wanted Sans | 깨끗한 선과 기하학적 형태가 특징인 모던하고 가독성 높은 서체. Wanted Dev 제작, SIL 오픈 폰트 라이선스로 상업적 사용·수정·재배포 가능 (폰트 단독 판매만 제외) |

**참고 후보 (미채택)**
- 카페24 아네모네 / 아네모네 에어 — 통통하고 존재감 있는 둥근 고딕체
- 여기어때 잘난체 / 잘난체 고딕 — 라운드와 직선이 조화된 브랜드 서체
- 메모먼트 꾹꾹체 — 손으로 눌러쓴 듯한 질감의 손글씨 감성 폰트 (상업적 사용 무료, 단 폰트 파일 수정·재배포 금지)

---

## 4. 레퍼런스 (References)

| 요소 | 레퍼런스 | 참고 포인트 |
|---|---|---|
| 전체 무드 · 카드 레이아웃 | LUVTTON (러브 다이어리 앱) | 따뜻한 톤의 카드형 UI, 기록 중심 레이아웃 |
| 저장 · 북마크 UX 흐름 | Whisk (레시피 저장 앱) | 콘텐츠를 카드로 저장하고 나중에 다시 찾아보는 구조 |
| 손글씨 · 메모 감성 | 손글씨 메모 앱, 저거바 다이어리 | 포스트잇 형태의 카드, 손글씨 폰트를 활용한 아날로그 감성 |
| 태그 필터 인터랙션 | 모바일 앱 필터 UI 패턴 | 태그를 누르면 필터링되는 인터랙션 |
| UI 형태 (미니멀 · 라운드) | 미니멀 라운드 버튼/카드 UI | 여백 중심 레이아웃 + 둥근 모서리로 부드러움 표현 |

---

## 5. 디자인 원칙 요약 (Design Principles)

- **무드**: 웜 & 코지 — 아늑하고 따뜻한 개인 아카이브 감성
- **레이아웃**: 미니멀 — 여백 우선, 장식 최소화
- **형태**: 라운드 — 카드/버튼/태그 모두 둥근 모서리 (과하지 않은 곡률)
- **컬러**: 크림 · 핑크 · 코랄레드 + 웜그레이 텍스트, 총 4색 이내로 절제
- **타이포**: 포인트(학교안심 둥근미소) + 본문(Wanted Sans) 조합으로 따뜻함과 가독성 동시 확보

---

## 6. 에어비앤비 영향 UI 시스템 업데이트 (Airbnb-Influenced System)

`index.html` 랜딩페이지 리디자인(2026-08)부터 반영. 브랜드 컬러·폰트·"웜&코지, 라운드" 무드는 그대로 유지하고, **레이아웃 실행 방식**에 에어비앤비 특유의 넉넉한 화이트스페이스·카드 기반 그리드·은은한 그림자·명확한 타이포 위계를 접목했다. 앞으로 새 화면(예: 맛집 담기, 리스트/필터 화면)도 아래 토큰을 기준으로 만든다.

### 6.1 레퍼런스 추가

| 요소 | 레퍼런스 | 참고 포인트 |
|---|---|---|
| 카드 그리드 · 화이트스페이스 · 신뢰감 있는 톤 | Airbnb (에어비앤비) 웹 | 넉넉한 여백, 카드 기반 리스팅, 은은한 그림자, 명확한 정보 위계, 정돈된 그리드 |

### 6.2 스페이싱 스케일 (8px 기준)

`4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96` (px). 섹션 내부 padding, 카드 내부 padding, 요소 간 gap 모두 이 스케일 값만 사용해 리듬을 통일한다. 임의의 px 값(예: 18px, 27px) 사용을 지양한다.

### 6.3 라운드 스케일

| 토큰 | 값 | 용도 |
|---|---|---|
| `--r-sm` | 12px | 버튼, 인풋, 작은 태그/칩 |
| `--r-md` | 16px | 아이콘 칩, 중간 카드 |
| `--r-lg` | 20px | 리스트/콘텐츠 카드 (기본) |
| `--r-xl` | 28px | 배너, 큰 CTA 카드, 히어로 요소 |

완전한 pill(반원)은 사용하지 않는다 — 기존 "동글동글하되 과하지 않은" 원칙 유지.

### 6.4 그림자(엘리베이션) 스케일

에어비앤비 카드처럼 "쉬는 상태는 거의 안 보일 만큼 은은하게, hover는 확실하게 뜨는" 대비를 사용한다.

| 토큰 | 값(예시) | 용도 |
|---|---|---|
| `--shadow-sm` | `0 1px 3px rgba(74,63,59,.08)` | 카드/칩/버튼 기본(쉬는) 상태 |
| `--shadow-md` | `0 10px 24px -10px rgba(74,63,59,.18)` | 살짝 띄운 표면 |
| `--shadow-lg` | `0 24px 48px -16px rgba(74,63,59,.24)` | CTA 배너, 모달 등 큰 표면 |
| `--shadow-accent` | `0 20px 40px -16px rgba(253,121,121,.45)` | 카드/태그 hover 시 코랄 톤 그림자 (브랜드 시그니처) |

카드는 기본적으로 `--shadow-sm` + 헤어라인 보더(`rgba(74,63,59,.10)`, 1px)를 함께 써서 흰 카드가 크림 배경 위에서도 선명하게 분리되도록 하고, hover 시 보더를 투명하게 없애면서 `--shadow-accent`로 전환 + `translateY(-4px)` 정도의 미세한 리프트를 준다.

### 6.5 그리드 · 컨테이너 (반응형 3단계 기준)

| 브레이크포인트 | 컨테이너 padding | 컨테이너 max-width | 카드 그리드 컬럼 |
|---|---|---|---|
| 모바일 (375 기준, ~767px) | 20px | 100% (fluid) | 1열 |
| 태블릿 (768px~) | 40px | 100% (fluid) | 2열 (홀수 개 마지막 카드는 전체 폭) |
| 데스크톱 (1440px~) | 40px | 1120px, 중앙 정렬 | 3열 이상 |

카드 grid는 `gap`도 스페이싱 스케일(모바일 16px → 태블릿 18px → 데스크톱 24px)을 따른다.

### 6.6 타이포 위계 정리

- 히어로/로고: `clamp(44px, 15vw, 88px)` — 화면 전체를 지배하는 단 하나의 대형 타이포
- 섹션 제목(H2): `clamp(24px, 6.5vw, 42px)`, MitmiFont
- 카드 제목(H3): 16~18px, Wanted Sans Bold
- 본문: 14~16px, Wanted Sans Regular/Medium, 웜그레이 72% 불투명도(`--muted`)로 본문·제목 대비를 명확히 분리
- 캡션/보조텍스트: 12~13px, 52% 불투명도(`--faint`)

이 위계를 지켜 "제목은 크고 확실하게, 본문은 절제해서" — 에어비앤비 특유의 명확한 정보 스캔 흐름을 만든다.

### 6.7 컴포넌트 패턴

- **내비게이션**: 상단 고정(fixed), 기본은 투명/반투명, 스크롤 시 크림 배경 + 블러(backdrop-filter) + 헤어라인 보더로 전환. 로고(좌) + 단일 CTA 버튼(우)만 두는 미니멀 구성.
- **버튼(Primary)**: 코랄 배경, 흰 텍스트, `--r-sm`, 기본 `--shadow-sm` → hover 시 `--shadow-accent` + 살짝 떠오르는 리프트.
- **버튼(Secondary, 밝은 배경 위)**: 흰 배경, 코랄 텍스트, 진한 배경(CTA 배너 등) 위에서 사용.
- **카드**: 흰 표면 + 헤어라인 보더 + `--shadow-sm` 기본, 아이콘은 크림 배경의 둥근 칩(`--r-md`) 안에 배치, 내부 padding은 스페이싱 스케일 준수.
- **태그/칩**: 흰 배경 + 핑크 보더가 기본, hover/active 시 코랄 배경 + 흰 텍스트로 전환 (필터 인터랙션에도 동일 패턴 적용).
