# PRD: 여기찜 (YeogiJjim)

## 1. 개요 (Overview)

**여기찜**은 인스타그램 등에서 발견한 맛집을 저장·관리하고, 혼밥·카공·데이트 등 "상황(분위기)"에 맞는 맛집을 빠르게 찾을 수 있도록 돕는 개인 맛집 아카이브 서비스다.

기존에는 인스타그램 자체 저장, 지도 앱 저장, 기억에만 의존하는 등 저장 채널이 여러 곳으로 흩어져 있어 나중에 다시 찾아보기 어려웠다. 여기찜은 이 저장 흐름을 하나로 통합하고, 모든 맛집에 "상황 태그"를 붙여 검색·필터링까지 한 번에 해결하는 것을 목표로 한다.

- **시작 형태**: 개인용 도구 (소희 1인 사용)
- **확장 계획**: 추후 여러 사용자가 함께 쓰는 서비스로 확장

---

## 2. 문제 정의 (Problem Statement)

1. 인스타그램에서 본 맛집을 저장하는 채널이 여러 개(인스타 저장, 지도 앱 저장, 저장 안 함)로 흩어져 있어 나중에 한 곳에서 찾아보기 어렵다.
2. 맛집을 찾을 때 "어떤 상황(혼밥/카공/데이트/가족모임 등)에 적합한지"가 가장 중요한 판단 기준인데, 기존 서비스(네이버지도, 망고플레이트 등)는 이런 상황 기반 필터링을 지원하지 않는다.

---

## 3. 목표 (Goals & Success Metrics)

- 인스타그램 저장 기능 대신 여기찜을 먼저 켜게 된다 (습관 대체)
- 네이버 지도보다 맛집을 찾고 기록하는 과정이 더 편리하다고 느낀다

---

## 4. 타겟 사용자 (Target User)

- **1차**: 소희 본인 (개인 아카이브 용도)
- **2차 (추후 확장)**: 인스타그램 등에서 맛집을 자주 발견하고 저장하지만, 상황별로 다시 찾아보기 어려워하는 사용자

---

## 5. 핵심 컨셉 (Core Concept)

> 모든 맛집(발견했든 저장했든)에 **상황 태그**가 붙고, 이 태그를 축으로 검색·필터링·저장 관리가 통합된다.

기존 인스타 저장 → 여기찜으로 옮기며 태깅 → 나중에 태그로 다시 찾아보기. 이 루프 자체가 서비스의 핵심 경험이다.

---

## 6. 핵심 기능 (Key Features)

### 6.1 맛집 등록 (Add Place)
- **입력 방식**
  - **메인**: 지도 링크(네이버지도/카카오맵) 붙여넣기 → 가게명, 주소, 카테고리 자동 파싱
  - **보조**: 수동 입력 (링크가 없거나 검색이 안 되는 경우)
- **기록 항목**
  | 항목 | 설명 |
  |---|---|
  | 방문 여부 | 안 가봄 / 다녀옴 |
  | 자유 메모 | 방문 전/후 상관없이 자유롭게 기록 (예: "웨이팅 길다던데", "파스타 맛있대") |
  | 가격대 | 1인 기준 지출 금액, 범위 선택형 (예: 1만원 이하 / 1~2만원 / 2~3만원 / 3만원 이상) |
  | 추천 메뉴 | 추천받은 메뉴 또는 방문 후 좋았던 메뉴 |
  | 상황 태그 | 아래 6.2 참고 |

### 6.2 상황 태그 (Situation Tags)
다중 선택 가능한 8개 고정 태그:

- 혼밥
- 카공
- 데이트
- 가족 모임
- 친구 모임
- 회식/술자리
- 기념일
- 비즈니스/미팅

### 6.3 리뷰 리스트 + 검색·필터
- 등록된 맛집을 리스트(JSON 기반 데이터)로 표시
- 상황 태그 1개를 선택하면 해당 태그가 붙은 맛집만 필터링되어 노출
- 검색 기능으로 가게명 등 빠르게 조회

### 6.4 AI 리뷰 요약 · 상황 태그 자동 추천 (Gemini API)
- 가게 등록 시, 관련 외부 리뷰를 AI가 분석하여 "카공 적합도", "혼밥 적합도" 등 상황 태그를 **자동으로 추천**
- 사용자는 추천된 태그를 확인 후 최종 선택/수정 (반자동 방식)
- ⚠️ **데이터 소스 관련 유의사항**: 네이버/카카오맵 리뷰는 공식 API 미제공 및 크롤링 시 약관 위반 소지가 있어, 구체적인 리뷰 수집 방식은 추후 별도 검토 필요. 현재는 기능 컨셉만 정의하고 데이터 소스 문제는 후속 과제로 남김.

### 6.5 로그인 및 데이터 저장 (Supabase)
- Supabase 기반 DB로 데이터 영구 저장 (기기 변경/브라우저 초기화에도 데이터 유지)
- MVP 단계: 로그인 기능 포함하여 처음부터 구현 (추후 멀티유저 확장을 고려한 구조)

### 6.6 UI/UX
- 스크롤·호버 애니메이션 적용
- 모바일 최적화 (반응형 웹: 모바일/데스크톱 모두 대응)

---

## 7. 추후 확장 기능 (Post-MVP / Future Scope)

- **도넛 차트 · 워드클라우드 대시보드**: 저장한 맛집의 태그 비율, 자주 쓴 키워드 등을 시각화하는 통계 화면
- **멀티유저 확장**: 여러 사용자가 각자 계정으로 사용하는 구조로 확장
- AI 리뷰 요약을 위한 외부 리뷰 데이터 수집 방식 구체화

---

## 8. 비범위 (Out of Scope, for now)

- 지역 제한 없음 — 별도의 지역 범위 제한을 두지 않음 (개인 아카이브 특성상 서울/특정 권역으로 좁힐 필요 없음)
- 크라우드소싱형 태깅 (여러 사용자가 함께 태그를 남기는 구조)는 현재 범위 밖

---

## 9. 기술 스택 (Tech Stack)

| 영역 | 기술 |
|---|---|
| 플랫폼 | 반응형 웹 |
| 데이터 저장 | Supabase (로그인 포함) |
| 데이터 포맷 | JSON |
| AI 기능 | Gemini API (리뷰 요약 · 감성분석) |
| 지도 정보 파싱 | 네이버지도 / 카카오맵 링크 |

---

## 10. 화면 구조 (Screen Structure)

> 추후 별도 논의 예정

---

## 11. 오픈 이슈 (Open Questions)

- AI 리뷰 요약을 위한 외부 리뷰 데이터 수집 방법 (약관 준수 방안 포함)
- 화면 구조(정보구조/IA) 상세 설계
- 디자인 방향(톤앤매너, 컬러, 타이포 등) 미정
