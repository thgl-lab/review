# 맛집 담기 (Place Search) — 디자인 스펙

> 이 문서는 **디자인 참고 자료**입니다. 기능 구현은 다른 teammate("logic")가 `place-search.html`(또는 별도 구현 파일)에서 카카오 로컬 API 기반으로 진행합니다. 이 문서와 `mockup-place-search.html`은 시각적 참고용이며, 실제 구현 파일이 아닙니다.
>
> 사용된 디자인 토큰(컬러/라운드/그림자/스페이싱)은 `DESIGN_YeogiJjim.md` 6장(에어비앤비 영향 UI 시스템)과 리디자인된 `index.html`을 기준으로 합니다. 새 토큰을 추가하지 않고 기존 시스템을 그대로 재사용합니다.

---

## 1. 목적 & 범위

PRD 6.1(맛집 등록)의 "메인 입력 방식" 중 **카카오 로컬 API 검색으로 가게를 찾아 담는 흐름**의 화면 디자인이다.

- 사용자가 키워드(또는 카테고리)로 맛집을 검색한다.
- 카카오 로컬 API 결과가 **카드형 리스트**로 나온다.
- 카드에서 바로 "담기"를 눌러 내 아카이브에 저장한다 (상황 태그·메모 등 상세 기록은 이 화면의 범위 밖 — 담기 이후 별도 상세/편집 플로우에서 진행한다고 가정).

## 2. 화면 흐름 (Flow)

```
[검색 진입]
   │
   ▼
검색창 포커스 + 카테고리 칩(선택) 노출
   │  (키워드 입력 또는 카테고리 칩 선택)
   ▼
로딩(스켈레톤 카드) ──▶ 결과 카드 리스트
   │                         │
   │                         ├─ 카드 "담기" 클릭 → 버튼이 "담았어요" 상태로 전환 (낙관적 UI)
   │                         └─ 카드 클릭(본문) → 상세 미리보기 또는 바로 상세 등록 화면으로 이동
   │
   ▼
결과 없음 → 빈 상태 화면
```

## 3. 레이아웃 (375 / 768 / 1440)

공통: 상단에 `index.html`과 동일한 고정 내비게이션(로고 + 홈으로 돌아가기 등 최소 구성)을 재사용해 브랜드 일관성을 유지한다. 본문 컨테이너는 `DESIGN_YeogiJjim.md` 6.5의 컨테이너 규칙을 그대로 따른다.

| 브레이크포인트 | 검색 영역 | 결과 카드 그리드 |
|---|---|---|
| 모바일 (375~) | 검색창 풀폭, 카테고리 칩은 가로 스크롤(overflow-x auto) 1줄 | 1열, 세로 스택 |
| 태블릿 (768~) | 검색창 + 칩 한 줄에 정렬 가능 | 2열 |
| 데스크톱 (1440~) | 검색창 최대폭 640px, 좌측 정렬 | 3열, 컨테이너 max-width 1120px |

카드 grid gap: 모바일 16px / 태블릿 18px / 데스크톱 24px (스페이싱 스케일 준수, `DESIGN_YeogiJjim.md` 6.5와 동일).

## 4. 검색바 컴포넌트

- 배경 `#FFFFFF`, 보더 없음, `--shadow-sm` 적용, radius `--r-sm`(12px)
- 높이: 48px(모바일) / 52px(태블릿·데스크톱)
- 좌측 검색 아이콘(🔍 또는 SVG), placeholder: `"맛집 이름이나 지역으로 검색해보세요"`
- 포커스 시: 보더 1.5px `var(--coral)` + `--shadow-accent`로 전환 (index.html 태그 hover와 동일한 상호작용 언어)
- 카테고리 칩(선택): 한식/카페/술집/일식/양식 등 — `index.html`의 `.tags-row span` 칩 스타일을 그대로 재사용 (흰 배경 + 핑크 보더 → 선택 시 코랄 배경 + 흰 텍스트)

## 5. 결과 카드 컴포넌트 — `.place-card`

### 5.1 앤커토미 (Anatomy)

```
┌─────────────────────────────────────┐
│  [카테고리 아이콘 칩]      [담기 버튼] │  ← 상단 행
│                                       │
│  가게명 (Bold, 17px)                 │
│  카테고리 · 거리(선택, 예: 도보 6분)   │  ← 메타 라인 (13px, --muted)
│                                       │
│  📍 주소 한 줄 (13px, --faint)        │
│                                       │
│  [상황 태그 칩 0~n개] (담긴 이후에만)  │  ← 선택적, 담기 전에는 노출 안 함
└─────────────────────────────────────┘
```

카카오 로컬 API 원본 결과에는 상황 태그가 없으므로, **담기 전 카드**에는 태그 자리가 없다. 담긴 뒤(내 아카이브 리스트에서) 같은 카드 컴포넌트를 재사용할 때만 상황 태그 칩 줄이 추가되는 구조를 권장한다 — 컴포넌트 재사용성을 위해 `.place-card` 하나로 "검색 결과 카드"와 "내 아카이브 카드"를 겸용할 수 있게 설계했다.

### 5.2 스펙 값

| 속성 | 값 |
|---|---|
| 배경 | `#FFFFFF` |
| 보더 | 1px solid `rgba(74,63,59,.10)` (헤어라인) |
| 라운드 | `--r-lg` (20px) |
| 그림자(기본) | `--shadow-sm` |
| 그림자(hover) | `--shadow-accent` + `translateY(-4px)` |
| 내부 padding | 20px (모바일) / 24px (태블릿·데스크톱) — 스페이싱 스케일 |
| 카테고리 아이콘 칩 | 40×40px, radius `--r-md`(16px), 배경 `var(--cream)` |
| 가게명 | 17px, Wanted Sans Bold, `var(--warmgray)`, 1줄 말줄임(ellipsis) |
| 메타 라인(카테고리·거리) | 13px, `--muted` |
| 주소 | 13px, `--faint`, 앞에 📍(또는 위치 아이콘) |
| 카드 간 gap | 4번(레이아웃) 항목 참고 |

### 5.3 담기 버튼 (카드 우상단, inline)

| 상태 | 배경 | 텍스트 | 아이콘 |
|---|---|---|---|
| 기본(미담김) | `var(--coral)` | `#fff`, "담기" | + |
| hover | `var(--coral)` 유지 + `--shadow-accent` | `#fff` | + |
| 담김(saved) | `#fff` + 1.5px solid `var(--pink)` | `var(--coral)`, "담았어요" | ✓ |
| 담김 hover(취소 유도) | `var(--pink)` 배경 | `var(--warmgray)`, "담기 취소" | × |

버튼 radius `--r-sm`(12px), padding `8px 14px`, font-size 13px, font-weight 700. 클릭 시 낙관적 UI로 즉시 상태 전환 후 실패 시에만 원복 (네트워크 대기 스피너로 사용자를 막지 않는다).

### 5.4 상태(States)

1. **기본(default)** — 위 스펙대로.
2. **hover** — `translateY(-4px)` + `--shadow-accent`, 보더 투명화. (`index.html` `.problem .card:hover`와 동일한 인터랙션 언어)
3. **담김(saved)** — 카드 전체에 `border-color: var(--coral)` 1.5px 유지해 "이미 담은 카드"임을 은은하게 표시. 담기 버튼은 5.3의 담김 상태로.
4. **로딩(skeleton)** — 카드와 동일한 크기·radius·shadow, 내부는 회색조 그러데이션 placeholder 블록(아이콘/제목/메타/주소 자리) + shimmer 애니메이션. 배경은 `#FFFFFF`, placeholder 블록 색은 `var(--cream)` 계열.
5. **결과 없음(empty)** — 카드 그리드 대신 중앙 정렬 블록: 아이콘(🔍) + "검색 결과가 없어요" + "다른 키워드로 다시 찾아볼까요?" 보조문구. `.problem .punch` 배너와 톤 통일 (핑크 배경 카드 형태 권장).
6. **에러(error)** — "잠시 후 다시 시도해주세요" + 재시도 버튼(Secondary 스타일). 사용자 탓이 아님을 명확히 하는 문구 사용.

## 6. 카피 가이드

- 버튼은 사용자가 실제로 하는 행동을 그대로 말한다: "담기" (저장/등록 아님), "담았어요" (완료 상태를 사용자 어투로).
- 빈 상태·에러 문구는 사과하지 않고, 다음 행동을 안내한다.
- 상황 태그·메모 등은 이 화면에서 요구하지 않는다 — 검색·담기 흐름을 가볍게 유지하고, 태깅은 별도 상세 편집 단계로 위임한다 (PRD 6.1/6.2 구조와 일치).

## 7. logic 팀 참고용 클래스 네이밍 제안

실제 구현 시 자유롭게 조정 가능하지만, 아래 이름을 기준으로 하면 `index.html`의 기존 클래스(`.container`, `--r-*`, `--shadow-*`, `--sp-*` 토큰)와 자연스럽게 맞물린다.

```html
<div class="place-card">
  <div class="place-card__top">
    <span class="place-card__icon">🍜</span>
    <button class="place-card__save-btn" data-state="default">+ 담기</button>
  </div>
  <h3 class="place-card__name">가게명</h3>
  <p class="place-card__meta">한식 · 도보 6분</p>
  <p class="place-card__address">📍 서울 마포구 …</p>
  <!-- 담긴 이후에만 노출 -->
  <div class="place-card__tags">
    <span class="tag">혼밥</span>
  </div>
</div>
```

토큰은 `index.html`의 `:root` 커스텀 프로퍼티(`--cream`, `--pink`, `--coral`, `--warmgray`, `--r-sm/md/lg/xl`, `--shadow-sm/md/lg/accent`, `--sp-*`)를 그대로 import/재사용하는 것을 권장한다 (동일 `<style>` 블록이든, 공유 CSS 파일이든 logic 팀 판단에 맡김).

## 8. 시각 참고

정적 목업은 `mockup-place-search.html`에서 확인할 수 있다 (검색바, 카드 default/hover/담김 상태, 스켈레톤, 빈 상태 포함).
